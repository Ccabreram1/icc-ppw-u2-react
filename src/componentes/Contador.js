import react from "react";

function Contador({ numero }) {
    return (
    <div>REACT {numero}
        <h1>Home</h1>
        <h2>Fundamentos</h2>
        <p>homepage works!</p>
    </div>
    );
}

export default Contador;